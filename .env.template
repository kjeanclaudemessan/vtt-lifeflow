# ═══════════════════════════════════════════════════════════════════════
# LifeFlow — Environment Variables Template
# ═══════════════════════════════════════════════════════════════════════
# Copy this file to .env and fill in your values:
#   cp .env.example .env
# ═══════════════════════════════════════════════════════════════════════

# ── Environment ──────────────────────────────────────────────────────
ENV=development

# ── Supabase (Local) ────────────────────────────────────────────────
SUPABASE_URL=http://localhost:54421
SUPABASE_ANON_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...
SUPABASE_SERVICE_ROLE_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...
SUPABASE_DB_URL=postgresql://postgres:postgres@localhost:54532/postgres

# ── Supabase (Staging — Coolify VPS) ───────────────────────────────
STAGING_SUPABASE_URL=https://supabase-staging-lifeflow.vttlife.com
STAGING_SUPABASE_DB_URL=postgresql://postgres:PASSWORD@144.91.67.237:5433/postgres
# STAGING_SUPABASE_ANON_KEY=        # see flutter/.env.staging
# STAGING_SERVICE_ROLE_KEY=          # see fastapi/.env.staging

# ── Supabase (Production — Coolify VPS) ────────────────────────────
# PROD_SUPABASE_URL=https://supabase-prod-lifeflow.vttlife.com
# PROD_SUPABASE_DB_URL=postgresql://postgres:PASSWORD@144.91.67.237:5434/postgres
# PROD_SUPABASE_ANON_KEY=
# PROD_SERVICE_ROLE_KEY=

# ── Supabase Access Token (for CLI) ────────────────────────────────
# SUPABASE_ACCESS_TOKEN=

# ── FastAPI ─────────────────────────────────────────────────────────
FASTAPI_SECRET_KEY=change-me-in-production
FASTAPI_DEBUG=true

# ── Coolify (replaces Fly.io) ───────────────────────────────────────
# COOLIFY_WEBHOOK_STAGING_API=https://...    # Coolify webhook URL for staging FastAPI
# COOLIFY_WEBHOOK_PROD_API=https://...       # Coolify webhook URL for production FastAPI

# ── Firebase (App Distribution) ────────────────────────────────────
# FIREBASE_APP_ID_ANDROID=
# FIREBASE_SERVICE_ACCOUNT_JSON=

# ── Apple / App Store ──────────────────────────────────────────────
# APPLE_ID=
# TEAM_ID=
# ITC_TEAM_ID=
# APP_STORE_CONNECT_KEY_ID=
# APP_STORE_CONNECT_ISSUER_ID=
# APP_STORE_CONNECT_API_KEY=  (base64 encoded .p8)
# MATCH_GIT_URL=
# MATCH_PASSWORD=

# ── Google Play ────────────────────────────────────────────────────
# GOOGLE_PLAY_JSON_KEY=  (base64 or path)

# ── Android Signing ───────────────────────────────────────────────
# ANDROID_KEYSTORE_BASE64=
# ANDROID_KEYSTORE_PASSWORD=
# ANDROID_KEY_PASSWORD=
# ANDROID_KEY_ALIAS=upload

# ── Shorebird (OTA) ───────────────────────────────────────────────
# SHOREBIRD_TOKEN=
